# Agent 工程执行规范

## 修改策略

- 优先做小步提交，避免大规模混杂改动。
- 变更应聚焦用户请求，不顺带修改无关逻辑。
- 兼容已有 CLI 参数和默认行为，除非需求明确要求调整。

## 文件写入约束

- 派生清单类 markdown 写入 `work-dir/<plan_stem>/context/`。
- 原始计划文件为只读输入，不允许回写。
- 新建文档优先放 `docs/` 对应子目录。
